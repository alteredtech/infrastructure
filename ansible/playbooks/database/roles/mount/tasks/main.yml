- name: create backup directory
  file:
    path: ${HOME}/database
    state: directory

- name: Mount backups for database
  become: yes
  ansible.posix.mount:
    src: "{{ mount_point }}"
    path: database
    opts: defaults,hard
    state: mounted
    fstype: nfs